@page
@model ComicTrackerApp.Pages.ComicsModel
@{
    ViewData["Title"] = "Collection";
}
<h1>Collection</h1>
<p><strong>Total Collection Value:</strong> $@Model.Comics.Where(c => c.InCollection).Sum(c => c.Price).ToString("F2")</p>
@using System.Linq
@foreach (var comic in Model.Comics.Where(c => c.InCollection))
{
    <img src="/images/@comic.ImageFileName" alt="@comic.Name" style="max-width: 300px;" />
    <p>@comic.Name</p>
    <p>@comic.Year</p>
    <p>Price: $@comic.Price.ToString("F2")</p>
    <hr>
}